import { reportStyles } from "./styles/report.styles.js";
import { tableStyles } from "./styles/table.styles.js";

export function allocationTemplate(data) {
    return `
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <title>Hostel Allocation Report</title>

    ${reportStyles}
    ${tableStyles}
</head>

<body>

    <div class="header">
        <h1>HOSTEL ALLOCATION REPORT</h1>
        <p>Hostel Allocation Management System</p>
    </div>

    <div class="info-grid">

        <div class="info-item">
            <strong>Academic Year:</strong>
            <span>${data.academicYear}</span>
        </div>

        <div class="info-item">
            <strong>Course:</strong>
            <span>${data.course}</span>
        </div>

        <div class="info-item">
            <strong>Year:</strong>
            <span>${data.year}</span>
        </div>

        <div class="info-item">
            <strong>Generated On:</strong>
            <span>${data.generatedAt}</span>
        </div>

        <div class="info-item">
            <strong>Total Applicants:</strong>
            <span>${data.totalApplicants}</span>
        </div>

        <div class="info-item">
            <strong>Allocated:</strong>
            <span>${data.allocatedCount}</span>
        </div>

        <div class="info-item">
            <strong>Not Allocated:</strong>
            <span>${data.notAllocatedCount}</span>
        </div>

    </div>

    <h2 class="section-title">
        Allocation Results
    </h2>

    <table>

        <thead>

            <tr>

                <th>Sr.</th>

                <th>Name</th>

                <th>Branch</th>

                <th>
                    ${data.year === "First Year" ? "Rank" : "CGPA"}
                </th>

                <th>Student Category</th>

                <th>Allocated Through</th>

                <th>Allocation Type</th>

                <th>Status</th>

            </tr>

        </thead>

        <tbody>

            ${data.results
                .map(
                    (student, index) => `
                <tr>

                    <td>${index + 1}</td>

                    <td>${student.name}</td>

                    <td>${student.branch}</td>

                    <td>${student.cgpa ?? "—"}</td>

                    <td>${student.studentCategory}</td>

                    <td>${student.allocatedCategory}</td>

                    <td>${student.allocationType || "Regular"}</td>

                    <td>
                        ${
                            student.allocated
                                ? "Allotted"
                                : "Not Allotted"
                        }
                    </td>

                </tr>
            `
                )
                .join("")}

        </tbody>

    </table>

    <div class="footer">
        <p>
            This report is system generated by the Hostel Allocation
            Management System.
        </p>

        <p>
            Generated on ${data.generatedAt}
        </p>
    </div>

</body>

</html>
`;
}
